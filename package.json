{
  "name": "devpulse",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "db:generate": "turbo run db:generate",
    "db:push": "turbo run db:push",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "cli:build": "turbo run build --filter=@devpulse/cli",
    "cli:dev": "pnpm --filter @devpulse/cli dev"
  },
  "devDependencies": {
    "turbo": "^2.3.0",
    "typescript": "^5.7.0"
  },
  "packageManager": "pnpm@9.0.0"
}
