{
  "name": "@devpulse/database",
  "version": "0.0.0",
  "private": true,
  "main": "./dist/client.js",
  "types": "./dist/client.d.ts",
  "scripts": {
    "build": "prisma generate && tsc",
    "db:generate": "prisma generate",
    "postinstall": "prisma generate",
    "db:push": "bash ../scripts/with-api-env.sh prisma db push",
    "db:studio": "bash ../scripts/with-api-env.sh prisma studio",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@prisma/client": "^6.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "prisma": "^6.0.0",
    "typescript": "^5.7.0"
  }
}
